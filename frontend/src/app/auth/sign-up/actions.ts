// Sign-up is now handled entirely client-side in page.tsx so the org can be
// created immediately after signup in the same browser session.
// This file is kept for compatibility but is no longer used.
export {};